# ========================================
# xxljob-enhance Environment Configuration
# ========================================
# This file contains all environment variables needed for Docker deployment.
# Copy this file to .env and update the values for your environment.

# ========================================
# JWT Configuration (REQUIRED)
# ========================================
# Secret key for signing JWT tokens (minimum 32 characters)
# Generate a secure secret: openssl rand -base64 32
JWT_SECRET=change-this-secret-in-production-minimum-32-characters-required

# JWT token expiration times
JWT_ACCESS_TOKEN_EXPIRATION=1h
JWT_REFRESH_TOKEN_EXPIRATION=7d

# ========================================
# XXL-Job Admin Configuration (REQUIRED)
# ========================================
# URL of your XXL-Job Admin instance
XXL_JOB_ADMIN_URL=http://localhost:8080/xxl-job-admin

# XXL-Job Admin credentials
XXL_JOB_ADMIN_USERNAME=admin
XXL_JOB_ADMIN_PASSWORD=123456

# ========================================
# CORS Configuration
# ========================================
# Allowed origins for CORS (comma-separated)
# For Docker deployment, use http://localhost or your domain
CORS_ORIGIN=http://localhost

# ========================================
# Port Configuration
# ========================================
# These are mapped in docker-compose.yml
# Backend: 3000:3000
# Frontend: 80:80

# ========================================
# Database Configuration
# ========================================
# For Docker deployment, database is persisted in a named volume
# No manual configuration needed - handled by docker-compose.yml

# ========================================
# Default Admin User (SECURITY IMPORTANT)
# ========================================
# Default administrator account created on first startup
# IMPORTANT: Change these credentials immediately after first login!
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123
DEFAULT_ADMIN_EMAIL=admin@example.com

# Enable/disable automatic database seeding
# Set to 'false' to disable automatic admin user creation
DATABASE_SEED=true
